APP_ID=ieta-dyna-snapshot
APP_NAME=IETA Dyna Snapshot
CAPABILITIES=
REQUIRED_SERVICES=
CONTAINER_NETWORK=ieta-znz-deploy
NOTES=Current inspected baseline uses SQLite/local file storage. Add minio/redis/postgres here only when the application actually externalizes those dependencies.
